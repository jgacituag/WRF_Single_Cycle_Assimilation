paths:
  prepared: /home/jorge.gacitua/datosdemerzel/wrf_python_Assimilation/data/ensemble_cross_sections.npz
  outdir:   /home/jorge.gacitua/datosdemerzel/wrf_python_Assimilation/data/runs/full2d_multicycle

state:
  truth_member: 0
  var_idx: {qg: 0, qr: 1, qs: 2, T: 3, P: 4}

obs:
  kind: RHI
  sigma_dbz: 1.0
  rhi:
    origin_x: 5          # grid x where the radar sits
    origin_z: 0          # typically ground at z=0
    angles_deg: [10, 20, 30, 40, 50, 60, 70]   # elevation angles
    max_range: 120       # in grid units (x/z cells)
    dr: 1.0              # gate spacing (grid units)

da:
  use_fortran_letkf: true
  cycles: 3
  ntemp: [3]
  alpha: [2]
  loc_scales: [5,5,5]

experiment_tag: full2d_multicycle_v1
